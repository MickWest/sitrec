// Paths for where we build the dev (development) and prod (production)
// versions of Sitrec
// For dev, this is a folder in the local web server
// for prod, it's a temporary folder that is then uploaded to the production server

module.exports = {
    dev_path: '/Users/mick/Library/CloudStorage/Dropbox/Metabunk/sitrec',
    prod_path: '/Users/mick/sitrec-deploy'
}